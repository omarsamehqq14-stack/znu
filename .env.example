# Node.js listens locally; Nginx exposes HTTPS/WSS publicly.
PORT=3000
# Comma-separated browser origins allowed to open WebSocket sessions.
# Example for GitHub Pages:
ALLOWED_ORIGINS=https://YOUR-USERNAME.github.io
